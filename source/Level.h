#include<vector>

class Level{
public:
    Level(LevelReader lvl)  //read in a level from file 
    bool isNextObjectReady() //indicates wether or not an object should be added to the game window
    ProtoObject getNextObject() //will return a protoObject of the next object which needs to be added to the game window
private:
    
};